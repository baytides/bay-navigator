---
import SiteLayout from '../layouts/SiteLayout.astro';
import yaml from 'js-yaml';
import fs from 'node:fs';
import path from 'node:path';

// --- Load all programs and count by category ---
const dataDir = path.join(process.cwd(), 'src/data');
const NON_PROGRAM_FILES = [
  'cities.yml',
  'groups.yml',
  'zipcodes.yml',
  'suppressed.yml',
  'search-config.yml',
  'transit-agencies.yml',
  'county-supervisors.yml',
  'site-config.yml',
  'bay-area-jurisdictions.yml',
  'city-profiles.yml',
  'helplines.yml',
  'custom-themes.yml',
  'chat-messages.yml',
  'homepage-pills.yml',
];

const suppressedFile = path.join(dataDir, 'suppressed.yml');
let suppressedIds = new Set<string>();
if (fs.existsSync(suppressedFile)) {
  const suppressedData = yaml.load(fs.readFileSync(suppressedFile, 'utf8')) as any[];
  if (Array.isArray(suppressedData)) {
    suppressedIds = new Set(suppressedData.map((s) => s.id));
  }
}

const categoryCounts: Record<string, number> = {};
let totalPrograms = 0;
const files = fs
  .readdirSync(dataDir)
  .filter((f) => f.endsWith('.yml') && !NON_PROGRAM_FILES.includes(f));

for (const file of files) {
  const content = fs.readFileSync(path.join(dataDir, file), 'utf8');
  const data = yaml.load(content) as any;
  if (!data) continue;
  const programs = Array.isArray(data) ? data : data.programs || [];
  for (const program of programs) {
    const category =
      program.category ||
      file
        .replace('.yml', '')
        .replace(/-/g, ' ')
        .replace(/\b\w/g, (c: string) => c.toUpperCase());
    const id =
      program.id ||
      `${category.toLowerCase().replace(/\s+/g, '-')}-${(program.name || '').toLowerCase().replace(/[^a-z0-9]+/g, '-')}`;
    if (!suppressedIds.has(id)) {
      categoryCounts[category] = (categoryCounts[category] || 0) + 1;
      totalPrograms++;
    }
  }
}

// --- Load contextual pills ---
const pillsFile = path.join(dataDir, 'homepage-pills.yml');
let pills: Array<{ label: string; detail: string; href: string; color: string; active: boolean }> =
  [];
try {
  const pillsData = yaml.load(fs.readFileSync(pillsFile, 'utf8')) as any;
  // Expiry is enforced here, not trusted to whoever edits the YAML. A stale
  // dated pill is worse than no pill: it is a visible claim that the site has
  // not been looked at since spring.
  const today = new Date().toISOString().slice(0, 10);
  pills = (pillsData?.pills || []).filter(
    (p: any) => p.active !== false && (!p.expires || String(p.expires) >= today)
  );
} catch {
  // Pills unavailable
}

// --- Load groups for eligibility card ---
const groupsFile = path.join(dataDir, 'groups.yml');
let groups: any[] = [];
try {
  const groupsData = yaml.load(fs.readFileSync(groupsFile, 'utf8')) as any;
  groups = groupsData?.groups || [];
} catch {}

// --- Load search config ---
let searchConfig = { synonyms: {}, best_bets: {}, suggestions: [], query_rewrites: {} };
try {
  const searchConfigPath = path.join(process.cwd(), 'src/data/search-config.yml');
  searchConfig = yaml.load(fs.readFileSync(searchConfigPath, 'utf8')) as typeof searchConfig;
} catch {}

// --- Load location data ---
let homeZipToCity: Record<string, string> = {};
let homeCityToCounty: Record<string, string> = {};
try {
  const citiesData = yaml.load(fs.readFileSync(path.join(dataDir, 'cities.yml'), 'utf8')) as Array<{
    name: string;
    county: string;
  }>;
  for (const city of citiesData) homeCityToCounty[city.name.toLowerCase()] = city.county;
  const zipData = yaml.load(fs.readFileSync(path.join(dataDir, 'zipcodes.yml'), 'utf8')) as Record<
    string,
    string
  >;
  homeZipToCity = Object.fromEntries(
    Object.entries(zipData).map(([zip, city]) => [String(zip), city])
  );
} catch {}

// --- Category counts ---
const foodCount = categoryCounts['Food'] || 0;
const utilityCount = categoryCounts['Utilities'] || 0;
const housingCount = categoryCounts['Housing'] || 0;
const healthCount = categoryCounts['Health'] || 0;
const jobsCount = categoryCounts['Employment'] || 0;
const legalCount = categoryCounts['Legal Services'] || 0;
const transitCount = categoryCounts['Transportation'] || 0;
const educationCount = categoryCounts['Education'] || 0;

// --- SVG icon paths (reusable) ---
const icons = {
  food: '<path stroke-linecap="round" stroke-linejoin="round" d="M15.75 10.5V6a3.75 3.75 0 1 0-7.5 0v4.5m11.356-1.993 1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 0 1-1.12-1.243l1.264-12A1.125 1.125 0 0 1 5.513 7.5h12.974c.576 0 1.059.435 1.119 1.007ZM8.625 10.5a.375.375 0 1 1-.75 0 .375.375 0 0 1 .75 0Zm7.5 0a.375.375 0 1 1-.75 0 .375.375 0 0 1 .75 0Z"/>',
  housing:
    '<path stroke-linecap="round" stroke-linejoin="round" d="m2.25 12 8.954-8.955c.44-.439 1.152-.439 1.591 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25"/>',
  health:
    '<path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z"/>',
  utilities:
    '<path stroke-linecap="round" stroke-linejoin="round" d="M12 18v-5.25m0 0a6.01 6.01 0 0 0 1.5-.189m-1.5.189a6.01 6.01 0 0 1-1.5-.189m3.75 7.478a12.06 12.06 0 0 1-4.5 0m3.75 2.383a14.406 14.406 0 0 1-3 0M14.25 18v-.192c0-.983.658-1.823 1.508-2.316a7.5 7.5 0 1 0-7.517 0c.85.493 1.509 1.333 1.509 2.316V18"/>',
  jobs: '<path stroke-linecap="round" stroke-linejoin="round" d="M20.25 14.15v4.25c0 1.094-.787 2.036-1.872 2.18-2.087.277-4.216.42-6.378.42s-4.291-.143-6.378-.42c-1.085-.144-1.872-1.086-1.872-2.18v-4.25m16.5 0a2.18 2.18 0 0 0 .75-1.661V8.706c0-1.081-.768-2.015-1.837-2.175a48.114 48.114 0 0 0-3.413-.387m4.5 8.006c-.194.165-.42.295-.673.38A23.978 23.978 0 0 1 12 15.75c-2.648 0-5.195-.429-7.577-1.22a2.016 2.016 0 0 1-.673-.38m0 0A2.18 2.18 0 0 1 3 12.489V8.706c0-1.081.768-2.015 1.837-2.175a48.111 48.111 0 0 1 3.413-.387m7.5 0V5.25A2.25 2.25 0 0 0 13.5 3h-3a2.25 2.25 0 0 0-2.25 2.25v.894m7.5 0a48.667 48.667 0 0 0-7.5 0M12 12.75h.008v.008H12v-.008Z"/>',
  transit:
    '<path stroke-linecap="round" stroke-linejoin="round" d="M8.25 18.75a1.5 1.5 0 0 1-3 0m3 0a1.5 1.5 0 0 0-3 0m3 0h6m-9 0H3.375a1.125 1.125 0 0 1-1.125-1.125V14.25m17.25 4.5a1.5 1.5 0 0 1-3 0m3 0a1.5 1.5 0 0 0-3 0m3 0h1.125c.621 0 1.129-.504 1.09-1.124a17.902 17.902 0 0 0-3.213-9.193 2.056 2.056 0 0 0-1.58-.86H14.25M16.5 18.75h-2.25m0-11.177v-.958c0-.568-.422-1.048-.987-1.106a48.554 48.554 0 0 0-10.026 0 1.106 1.106 0 0 0-.987 1.106v7.635m12-6.677v6.677m0 4.5v-4.5m0 0h-12"/>',
  alerts:
    '<path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126ZM12 15.75h.007v.008H12v-.008Z"/>',
  eligibility:
    '<path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75 11.25 15 15 9.75M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z"/>',
  airplane:
    '<path stroke-linecap="round" stroke-linejoin="round" d="M6 12 3.269 3.125A59.769 59.769 0 0 1 21.485 12 59.768 59.768 0 0 1 3.27 20.875L5.999 12Zm0 0h7.5"/>',
};

// --- Categories for the homepage grid ---
//
// Derived from the data, not hand-listed. The previous version hardcoded eight
// tiles against category names that had drifted ("Legal Services" when the data
// says "Legal"), so Legal Help silently vanished from the homepage while
// Recreation — the single biggest category at 203 programs — was never shown at
// all. Anything with programs now gets a tile.
const CATEGORY_META: Record<string, { blurb: string; icon: string }> = {
  Food: { blurb: 'Food banks, CalFresh, free meals and grocery help.', icon: icons.food },
  Housing: { blurb: 'Shelter, rent and deposit help, eviction defense.', icon: icons.housing },
  Health: { blurb: 'Medi-Cal, free clinics, dental and mental health.', icon: icons.health },
  Utilities: {
    blurb: 'CARE, FERA and LIHEAP discounts on power and water.',
    icon: icons.utilities,
  },
  Employment: { blurb: 'Job search, training and unemployment benefits.', icon: icons.jobs },
  Legal: { blurb: 'Free legal aid, immigration and tenant rights.', icon: icons.eligibility },
  Transportation: { blurb: 'Discount transit passes and paratransit rides.', icon: icons.transit },
  Education: { blurb: 'Financial aid, adult school and childcare help.', icon: icons.eligibility },
  Recreation: {
    blurb: 'Free museum days, parks, pools and community classes.',
    icon: icons.airplane,
  },
  'Federal Benefits': {
    blurb: 'Social Security, SSI, SNAP, Medicare and VA programs.',
    icon: icons.eligibility,
  },
  Community: {
    blurb: 'Community centers, volunteering and neighbor support.',
    icon: icons.housing,
  },
  Retail: { blurb: 'Low-income discounts on everyday goods and services.', icon: icons.food },
  Finance: { blurb: 'Free tax prep, banking and debt help.', icon: icons.jobs },
  'Library Resources': {
    blurb: 'Library cards, free wifi, computers and classes.',
    icon: icons.eligibility,
  },
  'Pet Resources': {
    blurb: 'Low-cost vet care, pet food banks and spay/neuter.',
    icon: icons.health,
  },
  Technology: {
    blurb: 'Affordable internet, free devices and digital skills.',
    icon: icons.utilities,
  },
  Lgbtq: { blurb: 'LGBTQ+ health, legal, housing and community support.', icon: icons.health },
  Safety: { blurb: 'Domestic violence, crisis lines and disaster help.', icon: icons.alerts },
  Equipment: { blurb: 'Free or loaned medical and mobility equipment.', icon: icons.health },
};

// --- Situations, not categories ---
//
// People do not arrive thinking "Utilities", they arrive thinking "my power is
// about to be shut off". These are the dozen openings that actually bring
// someone here, pointed at the search or category that answers them.
const situations = [
  {
    label: 'I need food today',
    detail: 'Food banks, free meals and pantries near you',
    href: '/directory?category=Food',
  },
  {
    label: "I'm behind on rent",
    detail: 'Rent help, eviction defense and emergency shelter',
    href: '/directory?category=Housing',
  },
  {
    label: "I can't pay a utility bill",
    detail: 'Discounts on power, gas, water and phone',
    href: '/directory?category=Utilities',
  },
  {
    label: 'I need to see a doctor',
    detail: 'Free clinics, Medi-Cal and mental health care',
    href: '/directory?category=Health',
  },
  {
    label: 'I need a lawyer',
    detail: 'Free legal aid, immigration and tenant rights',
    href: '/directory?category=Legal',
  },
  {
    label: "I'm looking for work",
    detail: 'Job training, placement and unemployment',
    href: '/directory?category=Employment',
  },
];

const DEFAULT_ICON = icons.eligibility;

const categories = Object.entries(categoryCounts)
  .filter(([, count]) => count > 0)
  .sort((a, b) => b[1] - a[1])
  .map(([name, count]) => ({
    name: name === 'Lgbtq' ? 'LGBTQ+' : name,
    count,
    href: `/directory?category=${encodeURIComponent(name)}`,
    blurb: CATEGORY_META[name]?.blurb || `${count} programs in this category.`,
    icon: CATEGORY_META[name]?.icon || DEFAULT_ICON,
  }));

// Pill color map
const pillColorMap: Record<string, string> = {
  amber: 'bg-amber-500',
  blue: 'bg-blue-500',
  emerald: 'bg-emerald-500',
  purple: 'bg-purple-500',
  red: 'bg-red-500',
};
---

<SiteLayout title="Home">
  <!-- ── Search ───────────────────────────────────────────────────────────── -->
  <section id="hero-section" class="lead">
    <div class="ds-shell lead__inner">
      <h1>Find free and low-cost help in the Bay&nbsp;Area.</h1>
      <p class="lead__sub">
        {totalPrograms.toLocaleString()} programs for food, housing, health care, utilities and more,
        across all nine counties. Nothing to sign up for.
      </p>

      <div class="finder" role="search">
        <div class="finder__field">
          <label for="search-input">What do you need?</label>
          <input id="search-input" type="search" autocomplete="off" enterkeyhint="search" />
          <p class="finder__hint">For example: food bank, help paying rent, free clinic</p>
        </div>

        <div class="finder__field">
          <label for="location-input">Where are you?</label>
          <div class="finder__row">
            <input
              id="location-input"
              type="text"
              autocomplete="postal-code"
              enterkeyhint="search"
            />
            <button id="detect-location-btn" type="button" class="finder__detect"
              >Use my location</button
            >
          </div>
          <p class="finder__hint">A city or ZIP code. Leave blank to search everywhere.</p>
        </div>

        <button id="search-submit-btn" type="button" class="finder__go">Search</button>
      </div>
    </div>
  </section>

  <!-- ── Start here ───────────────────────────────────────────────────────── -->
  <!-- The single biggest gap in the bare version: people do not arrive with a
       category in mind, they arrive with a situation. -->
  <section class="ds-shell block" aria-labelledby="start-h">
    <h2 id="start-h">Start here</h2>
    <ul class="tasks">
      {
        situations.map((t) => (
          <li>
            <a href={t.href}>
              <span class="tasks__q">{t.label}</span>
              <span class="tasks__a">{t.detail}</span>
            </a>
          </li>
        ))
      }
    </ul>
  </section>

  <!-- ── Urgent ───────────────────────────────────────────────────────────── -->
  <section class="ds-shell block" aria-labelledby="urgent-h">
    <div class="urgent">
      <h2 id="urgent-h">If you need help right now</h2>
      <ul>
        <li><a href="tel:911"><strong>911</strong> <span>Emergency</span></a></li>
        <li>
          <a href="tel:988"><strong>988</strong> <span>Suicide &amp; crisis lifeline</span></a>
        </li>
        <li>
          <a href="tel:211"><strong>211</strong> <span>Local resource helpline, 24 hours</span></a>
        </li>
      </ul>
    </div>
  </section>

  <!-- ── Live ─────────────────────────────────────────────────────────────── -->
  <section class="ds-shell block" aria-labelledby="live-h">
    <h2 id="live-h">Right now in the Bay Area</h2>
    <ul class="live">
      <li>
        <a href="/transit">
          <strong>Transit</strong>
          <span>Live BART, Muni and Caltrain status, plus discount fare programs.</span>
        </a>
      </li>
      <li>
        <a href="/alerts">
          <strong>Alerts</strong>
          <span>Missing persons, emergencies and community safety notices.</span>
          <span id="home-alerts-cases" class="live__cases"></span>
        </a>
      </li>
      <li>
        <a href="/airports">
          <strong>Airports</strong>
          <span>SFO, OAK and SJC — ground transport, parking and flight status.</span>
        </a>
      </li>
    </ul>
  </section>

  <!-- ── Everything ───────────────────────────────────────────────────────── -->
  <section class="ds-shell block" aria-labelledby="browse-h">
    <h2 id="browse-h">Browse everything</h2>
    <p class="block__sub">
      All {totalPrograms.toLocaleString()} programs, grouped by what they help with.
    </p>
    <ul class="cats">
      {
        categories.map((c) => (
          <li>
            <a href={c.href}>
              <span>{c.name}</span>
              <span class="cats__n">{c.count}</span>
            </a>
          </li>
        ))
      }
    </ul>
  </section>

  <!-- ── Who you are ──────────────────────────────────────────────────────── -->
  {
    groups.length > 0 && (
      <section class="ds-shell block" aria-labelledby="who-h">
        <h2 id="who-h">Programs for people like you</h2>
        <p class="block__sub">
          Not sure what you qualify for? <a href="/eligibility">Answer a few questions</a> — about
          two minutes, and nothing you enter leaves your device.
        </p>
        <ul class="who">
          {groups.slice(0, 10).map((g) => (
            <li>
              <a href={`/directory?group=${encodeURIComponent(g.id || g.name)}`}>{g.name}</a>
            </li>
          ))}
        </ul>
      </section>
    )
  }

  <div
    id="home-search-config"
    hidden
    data-synonyms={JSON.stringify(searchConfig.synonyms || {})}
    data-best-bets={JSON.stringify(searchConfig.best_bets || {})}
    data-query-rewrites={JSON.stringify(searchConfig.query_rewrites || {})}
    data-zip-to-city={JSON.stringify(homeZipToCity)}
    data-city-to-county={JSON.stringify(homeCityToCounty)}
    data-total-programs={String(totalPrograms)}
  >
  </div>

  <script is:inline src="/assets/js/home-search.js"></script>

  <script>
    import { makeSearchFn } from '@/lib/search-client';
    import { resolveLocationInput, buildCountiesFilter } from '@/lib/location';
    import { detectCounty } from '@/lib/geo-county';

    const cfg = document.getElementById('home-search-config');
    if (cfg && window.HomeSearch) {
      const searchConfig = {
        synonyms: JSON.parse(cfg.dataset.synonyms || '{}'),
        best_bets: JSON.parse(cfg.dataset.bestBets || '{}'),
        query_rewrites: JSON.parse(cfg.dataset.queryRewrites || '{}'),
      };
      const cityToCounty = JSON.parse(cfg.dataset.cityToCounty || '{}');
      window.HomeSearch.init({
        synonyms: searchConfig.synonyms,
        bestBets: searchConfig.best_bets,
        queryRewrites: searchConfig.query_rewrites,
        zipToCity: JSON.parse(cfg.dataset.zipToCity || '{}'),
        cityToCounty,
        totalPrograms: cfg.dataset.totalPrograms || '0',
        searchFn: makeSearchFn('/data/search-index.json', searchConfig, cityToCounty),
        resolveLocationFn: resolveLocationInput,
        buildCountiesFilterFn: buildCountiesFilter,
        // GPS -> county (point-in-polygon), not GPS -> distance: only ~30% of
        // programs carry coordinates, so distance ranking would bury the
        // statewide help that is half the directory.
        detectCountyFn: detectCounty,
      });
    }
  </script>

  <style>
    /* Composition only. Colour, type and spacing come from the design tokens. */
    .lead {
      padding-block: var(--ds-gap-xl) var(--ds-gap-lg);
    }
    .lead__inner {
      max-width: 52rem;
    }
    .lead h1 {
      margin: 0 0 var(--ds-gap);
      font-size: clamp(2.25rem, 1.7rem + 2.8vw, 3.5rem);
      max-width: 18ch;
    }
    .lead__sub {
      margin: 0 0 var(--ds-gap-lg);
      font-size: var(--ds-text-lg);
      color: var(--ds-text-muted);
    }

    .finder {
      border-top: 4px solid var(--ds-text);
      padding-top: var(--ds-gap-lg);
    }
    .finder__field {
      margin-bottom: var(--ds-gap-md);
      max-width: 34rem;
    }
    .finder__field label {
      display: block;
      font-size: var(--ds-text-base);
      font-weight: 700;
      margin-bottom: var(--ds-gap-2xs);
    }
    .finder input {
      width: 100%;
      min-height: 52px;
      padding: 0.625rem 0.75rem;
      font: inherit;
      color: var(--ds-text);
      background: var(--ds-bg);
      border: 2px solid var(--ds-text);
      border-radius: 0;
    }
    .finder__row {
      display: flex;
      gap: var(--ds-gap-xs);
      flex-wrap: wrap;
    }
    .finder__row input {
      flex: 1 1 14rem;
      min-width: 0;
    }
    .finder__detect {
      min-height: 52px;
      padding-inline: 1rem;
      font: 600 var(--ds-text-sm) / 1 inherit;
      color: var(--ds-text);
      background: var(--ds-bg);
      border: 2px solid var(--ds-text);
      cursor: pointer;
    }
    .finder__detect:hover {
      background: var(--ds-surface-2);
    }
    .finder__hint {
      margin: var(--ds-gap-2xs) 0 0;
      font-size: var(--ds-text-sm);
      color: var(--ds-text-muted);
    }
    .finder__go {
      min-height: 52px;
      padding: 0.75rem 1.75rem;
      font: 700 var(--ds-text-base) / 1 inherit;
      color: var(--ds-text-on-brand);
      background: var(--ds-brand);
      border: 0;
      box-shadow: 0 3px 0 #0b1f57;
      cursor: pointer;
    }
    .finder__go:hover {
      background: #0f2f85;
    }

    .block {
      padding-block: var(--ds-gap-lg);
    }
    .block h2 {
      margin: 0 0 var(--ds-gap-xs);
    }
    .block__sub {
      margin: 0 0 var(--ds-gap);
      color: var(--ds-text-muted);
    }
    .block__sub a {
      color: var(--ds-brand-strong);
      font-weight: 600;
    }

    ul {
      list-style: none;
      margin: 0;
      padding: 0;
    }

    /* Situations, not categories. Two lines each so the second line can carry
       the concrete promise rather than leaving the label to do all the work. */
    .tasks {
      display: grid;
      gap: 0;
      border-top: 1px solid var(--ds-border);
    }
    @media (min-width: 52rem) {
      .tasks {
        grid-template-columns: 1fr 1fr;
        column-gap: var(--ds-gap-lg);
      }
    }
    .tasks li {
      border-bottom: 1px solid var(--ds-border);
    }
    .tasks a {
      display: block;
      min-height: 64px;
      padding-block: var(--ds-gap-sm);
      text-decoration: none;
    }
    .tasks__q {
      display: block;
      color: var(--ds-brand-strong);
      font-size: var(--ds-text-lg);
      font-weight: 650;
      text-decoration: underline;
      text-underline-offset: 3px;
    }
    .tasks__a {
      display: block;
      color: var(--ds-text-muted);
      font-size: var(--ds-text-sm);
    }

    .urgent {
      background: var(--ds-surface);
      border-left: 10px solid var(--ds-danger);
      padding: var(--ds-gap-md);
    }
    .urgent h2 {
      margin-top: 0;
    }
    .urgent a {
      display: flex;
      align-items: baseline;
      gap: var(--ds-gap-sm);
      min-height: var(--ds-tap);
      padding-block: var(--ds-gap-2xs);
      color: var(--ds-brand-strong);
      font-size: var(--ds-text-base);
    }
    .urgent strong {
      min-width: 3.25rem;
      font-size: var(--ds-text-xl);
      color: var(--ds-text);
    }

    .live {
      display: grid;
      gap: var(--ds-gap);
      grid-template-columns: repeat(auto-fit, minmax(min(100%, 17rem), 1fr));
    }
    .live a {
      display: flex;
      flex-direction: column;
      gap: var(--ds-gap-2xs);
      height: 100%;
      padding: var(--ds-gap);
      border: 1px solid var(--ds-border);
      text-decoration: none;
      color: var(--ds-text-muted);
    }
    .live a:hover {
      box-shadow: inset 0 0 0 1px var(--ds-brand);
      border-color: var(--ds-brand);
    }
    .live strong {
      color: var(--ds-brand-strong);
      font-size: var(--ds-text-lg);
      text-decoration: underline;
      text-underline-offset: 3px;
    }
    .live__cases {
      font-size: var(--ds-text-sm);
    }

    /* A list, not a tile grid. Nineteen labels down aligned columns scan far
       faster than the same nineteen scattered across a ragged 3-up grid. */
    .cats {
      columns: 1;
      column-gap: var(--ds-gap-xl);
      border-top: 1px solid var(--ds-border);
    }
    @media (min-width: 40rem) {
      .cats {
        columns: 2;
      }
    }
    @media (min-width: 64rem) {
      .cats {
        columns: 3;
      }
    }
    .cats li {
      break-inside: avoid;
      border-bottom: 1px solid var(--ds-border);
    }
    .cats a {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: var(--ds-gap);
      min-height: 52px;
      padding-block: var(--ds-gap-xs);
      color: var(--ds-brand-strong);
      font-weight: 600;
      text-decoration: underline;
      text-underline-offset: 3px;
    }
    .cats__n {
      color: var(--ds-text-muted);
      font-weight: 400;
      font-variant-numeric: tabular-nums;
    }

    .who {
      display: flex;
      flex-wrap: wrap;
      gap: var(--ds-gap-xs);
    }
    .who a {
      display: inline-flex;
      align-items: center;
      min-height: var(--ds-tap);
      padding-inline: var(--ds-gap);
      border: 1px solid var(--ds-border);
      color: var(--ds-text);
      font-weight: 600;
      text-decoration: none;
    }
    .who a:hover {
      background: var(--ds-surface-2);
    }
  </style>
</SiteLayout>
