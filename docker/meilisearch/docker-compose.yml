services:
  meilisearch:
    image: getmeili/meilisearch:latest
    container_name: bay-navigator-meilisearch
    restart: unless-stopped
    ports:
      - '7700:7700'
    environment:
      MEILI_MASTER_KEY: '${MEILI_MASTER_KEY}'
      MEILI_ENV: production
    volumes:
      - ./data:/meili_data
